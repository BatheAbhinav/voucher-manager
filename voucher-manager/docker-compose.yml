services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: voucher_manager
      POSTGRES_USER: voucher_manager
      POSTGRES_PASSWORD: voucher_manager
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
